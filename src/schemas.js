export const foodsSchema = {
    name: String,
    calories: Number,
    totalFat: Number,
    saturatedFat: Number,
    polyunsaturatedFat: Number,
    monounsaturatedFat: Number,
    transFat: Number,
    cholestorol: Number,
    sodium: Number,
    potassium: Number,
    totalCarbs: Number,
    dietaryFiber: Number,
    sugars: Number,
    protein: Number,
    vitaminA: Number,
    vitaminC: Number,
    calcium: Number,
    iron: Number
};

export const usersSchema = {
    name: String,
    password: String,
    weight: Number,
    height: Number
}